# Module02 FAQ

## Q. `rm -rf` は使っていいですか？

使えますが、実行前に必ず `pwd` と `ls -lah` を確認してください。

## Q. `cat` と `less` はどう使い分けますか？

短いファイルは `cat`、長いファイルは `less` が向いています。

## Q. ログを見るなら何を使いますか？

まずは `tail -f` を使います。
